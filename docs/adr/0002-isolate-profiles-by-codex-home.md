# Isolate account profiles by Codex Home

Each Limitr Account Profile maps to a distinct Codex Home and interacts with Codex inside that boundary. This makes multi-account behavior explicit and prevents Limitr from copying or interpreting credentials, though each simultaneously monitored profile may require its own local app-server process.

