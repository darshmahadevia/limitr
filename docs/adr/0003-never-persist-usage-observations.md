# Never persist usage observations

Limitr will persist profile locations, labels, and harmless display preferences, but no limit snapshots, graph samples, or token activity. A compact graph is built from a bounded Live Trace held only for the lifetime of the process; this sacrifices cross-launch trends in exchange for a simple, privacy-preserving present-tense monitor with no usage database or history.

